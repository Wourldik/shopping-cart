import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { ReactiveFormsModule } from '@angular/forms';

export const common = [CommonModule, ReactiveFormsModule, RouterModule];
